# 店舗回転率シミュレーター - 使用ガイド

## 概要
このシミュレーターは、Streamlit と Pymunk を使用して、100人の客が店舗内を移動する様子をリアルタイムで可視化します。

## 特徴

### 1. 物理演算エンジン (Pymunk)
- 客同士の衝突検出と回避
- 壁（床面画像の黒いピクセル）との衝突判定
- リアルな動き

### 2. 客の行動フロー
各客は以下の順序で行動します：

1. **入店** - 画像右下（x=0.8, y=0.9）から入店
2. **特売エリア** - 黄色で表示された特売エリアで5秒間滞在
3. **棚1** - ランダムに選ばれた最初の棚で3秒間滞在
4. **棚2** - ランダムに選ばれた2番目の棚で3秒間滞在
5. **レジ** - 最も空いているレジに並び、会計（2～5秒）
6. **退店** - 画像左下（x=0.2, y=0.9）から退店

### 3. 個人差
- 各客は0.8倍～1.2倍のランダムな速度を持つ
- これにより、リアルな人の流れが再現される

### 4. レジシステム
- 5つのレジが画像下部中央に配置
- 客は最も空いているレジを自動選択
- レジの待ち行列を管理

## 操作方法

### メイン画面

#### 制御ボタン
- **▶️ 開始 / ⏸️ 一時停止**: シミュレーションの開始と一時停止を切り替え
- **🔄 リセット**: シミュレーションを初期状態に戻す

#### 統計情報
- **⏱️ 経過時間**: シミュレーション開始からの時間（秒）
- **🚶 現在の店内人数**: 現在店内にいる客の数
- **✅ 累計退店数**: これまでに退店した客の総数

#### シミュレーション画面
- **白い床**: 通行可能エリア
- **黒い壁**: 通行不可エリア
- **青い点**: 客（100人）
- **黄色い円**: 特売エリア
- **緑色の点**: レジ（5箇所）

#### 退店数グラフ
- 時間経過に応じた累計退店数を折れ線グラフで表示
- シミュレーションの効率を視覚的に確認

### サイドバー

#### シミュレーション速度
- スライダーで1.0x ～ 5.0x の範囲で調整可能
- 速度を上げると、より短時間で結果を確認可能

#### 特売エリア位置
- **X座標**: 0.1 ～ 0.9 の範囲で左右の位置を調整
- **Y座標**: 0.1 ～ 0.6 の範囲で上下の位置を調整
- リアルタイムで位置が変更され、客の動きが影響を受ける

## 技術仕様

### システム要件
- Python 3.8 以上
- 必要なライブラリ（requirements.txt に記載）

### 座標系
- 画像サイズ: 800x600 ピクセル
- 座標は相対値（0.0 ～ 1.0）で指定
- 左上が原点 (0, 0)、右下が (1, 1)

### 主要な定数
- 客の半径: 8 ピクセル
- 壁の閾値: RGB値が50未満のピクセルを壁として認識
- フレームレート: 30 FPS

## トラブルシューティング

### シミュレーションが動かない
1. ▶️ 開始ボタンを押したか確認
2. floor_plan.jpg が存在するか確認
3. 依存ライブラリが正しくインストールされているか確認

### 客が動かない／壁を通り抜ける
- 壁の検出が正しく行われているか確認
- floor_plan.jpg の黒いピクセルが十分に暗いか確認（RGB < 50）

### 動作が重い
- シミュレーション速度を下げる
- 壁のサンプリングレートを上げる（app.py の sample_rate を変更）

## カスタマイズ

### 客の数を変更
app.py の以下の箇所を変更：
```python
if st.session_state.customers_created < 100:  # ← ここを変更
```

### 滞在時間を変更
各状態の滞在時間は app.py の `update_customer_state` メソッド内で変更可能：
- 特売エリア: 5.0秒
- 棚1: 3.0秒
- 棚2: 3.0秒
- レジ: 2.0～5.0秒（ランダム）

### 床面レイアウトを変更
floor_plan.jpg を別の画像に置き換えることで、任意の店舗レイアウトに対応可能。
黒い部分が壁として認識される。

## 活用例

### 1. 特売エリアの最適配置
- 特売エリアの位置を変更し、退店時間への影響を観察
- 店舗奥に配置すると客の滞在時間が増加

### 2. レジ配置の検証
- レジの数や位置を変更し、待ち時間を最適化
- コードを修正してレジ数を変更可能

### 3. 通路幅の検証
- floor_plan.jpg の通路幅を変更し、混雑度を確認
- 狭い通路は渋滞を引き起こす

### 4. ピークタイムのシミュレーション
- 客の入店間隔を変更してピークタイムを再現
- app.py の `elapsed_time > st.session_state.customers_created * 0.5` を変更

## さらに詳しく

技術的な詳細や実装の詳細については、以下のファイルを参照してください：
- `app.py` - メインアプリケーション
- `test_simulation.py` - テストスクリプト
- `README.md` - プロジェクト概要
